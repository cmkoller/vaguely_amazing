= form_for [:admin, @post] do |f|
  = f.label :title
  = f.text_field :title
  = f.label :body
  = f.text_area :body, class: "redactor-fancy", rows: 40, cols: 120
  = f.submit "Submit", class: "button"
